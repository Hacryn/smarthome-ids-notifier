#pragma once

// Sec. 12.2 - firmware version shown in /config, <major>.<minor>.<patch>.
// No automatic bump mechanism: increment by hand on a significant change.
inline constexpr const char* FIRMWARE_VERSION = "2.0.0";
